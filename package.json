{
  "name": "card-explorer",
  "version": "1.0.0",
  "private": true,
  "description": "Deck & Rules — an offline-first reference for 57 card and party games",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "npx http-server -p 8099 -c-1 .",
    "validate": "node tools/validate.mjs",
    "smoke": "node tools/smoke.mjs",
    "icons": "node tools/make-icons.mjs",
    "test": "npm run validate && npm run smoke"
  },
  "comment": "No dependencies on purpose. Static hosts run `npm install` when they see this file, and anything listed here lands in the deploy. The smoke tests need Playwright — install it yourself when you want to run them: npm i -D playwright && npx playwright install chromium"
}
